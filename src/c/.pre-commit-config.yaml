repos:
  - repo: local
    hooks:
      - id: clang-format
        name: clang-format
        entry: clang-format
        language: system
        types: [c]
      - id: clang-tidy
        name: clang-tidy
        entry: clang-tidy
        language: system
        types: [c]
